compilação arquivos:
	gcc -g -Wall -O0 -std=c99 *.c

para linux adicionar:
	-lm

execução:
	./main nome_arquivo.class imprimir // para exibicao do arquivo .class
	./main nome_arquivo.class executar // para execucao do arquivo .class
